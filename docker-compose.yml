services:
  db:
    image: pgvector/pgvector:pg17-bookworm
    environment:
      POSTGRES_HOST_AUTH_METHOD: trust
      POSTGRES_DB: yara
    ports:
      - "8888:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata: